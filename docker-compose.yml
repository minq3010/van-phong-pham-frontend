version: '3.8'

services:
  frontend:
    build: .
    container_name: frontend_dev
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000/api
    volumes:
      - .:/app
      - /app/node_modules
